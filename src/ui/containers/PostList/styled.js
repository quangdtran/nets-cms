import styled from 'styled-components';

export const WrapPosts = styled.div`
  margin: 32px;
  color: ${({ theme }) => theme.color.textMainContent};
  /* background-color: ${({ theme }) => theme.bgColor.white}; */
`;
